<!-- Validation failure messages -->
<% if @prop.errors.any? %>
  <% @prop.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Prop #<%= @prop.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">

    <form action="/update_prop/<%= @prop.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">



      <!-- Label and input for saladgram_id -->

      <div class="form-group">
        <label for="saladgram_id" class="control-label">
          Saladgram
        </label>


        <input type="text" id="saladgram_id" name="saladgram_id" placeholder="Saladgram" class="form-control" value="<%= @prop.saladgram_id %>">

      </div>



      <!-- Label and input for comment -->

      <div class="form-group">
        <label for="comment" class="control-label">
          Comment
        </label>


        <input type="text" id="comment" name="comment" placeholder="Comment" class="form-control" value="<%= @prop.comment %>">

      </div>



      <button class="btn btn-warning">
        Update Prop
      </button>
      or

      <a href="/props/<%= @prop.id %>">Cancel</a>


    </form>

  </div>
</div>
